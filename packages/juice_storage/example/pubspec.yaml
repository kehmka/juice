name: juice_storage_example
description: Demo app for StorageBloc - TTL, lazy eviction, cache cleanup, live event log.
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  juice:
    path: ../../juice
  juice_storage:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
