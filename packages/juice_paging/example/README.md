# juice_paging example

An infinite-scroll feed, built with Juice primitives only.

A fake fetcher serves 95 items in pages of 20 (with a simulated delay) — no
backend. Scroll to the bottom to `loadMore`; pull down to `refresh`. The footer
shows a spinner while loading, "— end —" when done.

## Run

```bash
flutter run
```
